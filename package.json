{
  "name": "cloud-garden-app-template",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "npx tsx scripts/deploy.ts"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "netlify-cli": "^17.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
